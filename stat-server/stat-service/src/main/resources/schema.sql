CREATE TABLE IF NOT EXISTS hits {
    id BIGINT GENERATED BY DEFAULT AS IDENTITY (START 1 INCREMENT BY 1) PRIMARY KEY,
    app VARCHAR(255),
    uri VARCHAR(1024),
    ip VARCHAR(15),
    timestamp TIMESTAMP WITHOUT TIME ZONE
}